config:
  target: "http://localhost:3000"
  phases:
    - duration: 300
      arrivalRate: 5



scenarios:
  - name: "connect"
    engine: "socketio"
    flow:
      - get:
          url: "/post"
      - emit:
          channel: "open posts"
          data: "john doe"
      - think: 300